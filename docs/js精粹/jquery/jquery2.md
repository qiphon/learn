# jquery 2.0.3

```js

;(function(win, undefined){
  
  // 定义 jQuery 函数
  jQuery = function(){}

  // jquery 的继承方法
  extend 

  // 扩展一些工具方法
  jQuery.extend()

  // 复杂选择器的实现
  Sizzle

  // 回调对象 函数统一管理
  Callbacks
  
  // 延迟对象 ： 对异步的统一管理
  Deferred

  // 功能检测
  support

  // 数据缓存
  data()

  // 队列
  queue （入队）
  dequeue (出队)

  // 元素属性操作
  attr()
  prop()
  addClass()等

  // 事件操作、
  on()
  trigger()

  // dom 操作 

  // 样式处理 
  css()

  // 数据和 Ajax load getJson

  // 运动操作
  animate 

  // 位置尺寸相关方法

  // jQuery 模块化支持

  // 将函数赋值给window （）
  window.jQuery = window.$ = jQuery;

})(window);

```